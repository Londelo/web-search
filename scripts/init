#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
REPO_DIR="$(cd "$SCRIPT_DIR/.." && pwd)"

echo "Cloning mcp-searxng..."
git clone https://github.com/Londelo/mcp-searxng "$REPO_DIR/mcp-searxng"

echo "Done."